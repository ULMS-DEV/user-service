# Avoid broken symlinks under paths with spaces (e.g. "Graduation Project")
node-linker=hoisted

# Allow all native dependency build scripts (prisma, @nestjs/core, grpc, argon2, …)
dangerouslyAllowAllBuilds=true

# Do not run "pnpm install" before every "pnpm run dev"
verifyDepsBeforeRun=false
